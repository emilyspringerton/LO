## 2026-08-30
- Real critical review of LoLanguageSpec.pdf (the real source design doc, a captured Gemini chat transcript). Founder real-time: "LO upstream and design doc added - check it over it needs to compile into java ts c and go via parena cli and burrow cli." Wrote NORTHSTAR.md: real gaps found (no formal grammar yet, emoji-tokenization ambiguity not addressed, loops/error-handling explicitly unfinished in the source material, a real code-size-blowup risk in the spec's own `let`-lowering scheme), a real architectural correction (LO/qi should emit real .prn text only, not its own C/TS/Java/Go backends -- reuse parena/burrow entirely), and a real, current capability audit (parena's own defstruct/defenum/match/loop/Vec support is real and mature; burrow's own two emitters are real but narrower -- scalar+flat-struct only, no let/construction/defenum/match/loop/Vec -- meaning only a scalar/flat-struct LO subset can reach Go via burrow today). Real, phased plan: Phase 0 (formal grammar) -> Phase 1 (LO-to-.prn, scalars/structs only, proven on both parena and burrow) -> Phase 2 (qi's own frontend, same scope) -> Phase 3+ (vectors/matrices/patterns/unions, gated on burrow's own real feature growth). No compiler code written yet. CLAUDE.md/README.md added, registered in root CLAUDE.md + golden-docs-index. (sess-20260830-0908-5ede695f)
- Phase 0 of NORTHSTAR.md's phased plan: wrote `GRAMMAR.md`, the real formal grammar the source spec never produced. Exact lexical token table (codepoints + variation-selector matching rule, closing the NORTHSTAR "emoji tokenization ambiguity" gap), an EBNF grammar for the ternary/expression language, precedence & associativity rules (ternary right-associative, no bare-state-as-condition), vector/matrix/row-extraction shape rules (N+1 STACK tokens per N-row matrix; empty vectors disallowed in favor of the dedicated VOID token), and a real fix for the GROUP capture-group glyph being non-distinguishing open/close (v1: non-nesting only). Re-derived 3 of the source doc's own worked examples against the grammar (§7) -- one parses clean, one (the casual early XOR example) turns up a genuine gap in the source's own worked example (a dangling AND4 operand), surfaced rather than silently patched. Explicitly deferred, not assumed: raw-state truthiness, unchecked-MATMUL rejection, the `let`-lowering blowup problem, loops/recursion/error-handling, and the EQ glyph's exact identity (source PDF's own text extraction lost it everywhere it appears; provisionally assigned 🟰, flagged for founder confirmation against the live chat transcript). No lexer/parser code written -- Phase 0 is grammar-only, matching Spec Before Implementation. (sess-20260830-1207-cc0ba7da)
