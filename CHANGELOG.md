## 2026-08-30
- Real critical review of LoLanguageSpec.pdf (the real source design doc, a captured Gemini chat transcript). Founder real-time: "LO upstream and design doc added - check it over it needs to compile into java ts c and go via parena cli and burrow cli." Wrote NORTHSTAR.md: real gaps found (no formal grammar yet, emoji-tokenization ambiguity not addressed, loops/error-handling explicitly unfinished in the source material, a real code-size-blowup risk in the spec's own `let`-lowering scheme), a real architectural correction (LO/qi should emit real .prn text only, not its own C/TS/Java/Go backends -- reuse parena/burrow entirely), and a real, current capability audit (parena's own defstruct/defenum/match/loop/Vec support is real and mature; burrow's own two emitters are real but narrower -- scalar+flat-struct only, no let/construction/defenum/match/loop/Vec -- meaning only a scalar/flat-struct LO subset can reach Go via burrow today). Real, phased plan: Phase 0 (formal grammar) -> Phase 1 (LO-to-.prn, scalars/structs only, proven on both parena and burrow) -> Phase 2 (qi's own frontend, same scope) -> Phase 3+ (vectors/matrices/patterns/unions, gated on burrow's own real feature growth). No compiler code written yet. CLAUDE.md/README.md added, registered in root CLAUDE.md + golden-docs-index. (sess-20260830-0908-5ede695f)
- Phase 0 of NORTHSTAR.md's phased plan: wrote `GRAMMAR.md`, the real formal grammar the source spec never produced. Exact lexical token table (codepoints + variation-selector matching rule, closing the NORTHSTAR "emoji tokenization ambiguity" gap), an EBNF grammar for the ternary/expression language, precedence & associativity rules (ternary right-associative, no bare-state-as-condition), vector/matrix/row-extraction shape rules (N+1 STACK tokens per N-row matrix; empty vectors disallowed in favor of the dedicated VOID token), and a real fix for the GROUP capture-group glyph being non-distinguishing open/close (v1: non-nesting only). Re-derived 3 of the source doc's own worked examples against the grammar (§7) -- one parses clean, one (the casual early XOR example) turns up a genuine gap in the source's own worked example (a dangling AND4 operand), surfaced rather than silently patched. Explicitly deferred, not assumed: raw-state truthiness, unchecked-MATMUL rejection, the `let`-lowering blowup problem, loops/recursion/error-handling, and the EQ glyph's exact identity (source PDF's own text extraction lost it everywhere it appears; provisionally assigned 🟰, flagged for founder confirmation against the live chat transcript). No lexer/parser code written -- Phase 0 is grammar-only, matching Spec Before Implementation. (sess-20260830-1207-cc0ba7da)
- Founder real-time: "continue working on lo adding to the stdlib libs necessary to make the language actually function... theoretically ffi into parena is acceptable if it satisfies the design." Built the real target for that direction in PARENA (not LO itself -- LO has no compiler yet, Phase 1): `PARENA/stdlib/base4/vector.prn` -- elementwise base4 vector ops (xor/and/or/add/subtract), structural equality, dimlen, dot product, matching GRAMMAR.md's §5.1/§5.3 operator set. Confirmed a real compiler bug on `dot` (loop-accumulator double-boxing, same class already documented in PARENA's own linalg.prn) rather than silently accepting it -- 24 assertions, `make test-base4-vector` green in PARENA, -Werror clean. Matrix (STACK/MATMUL) and the PCRE-lite pattern matcher (§5.2/§5.4) deliberately deferred as named follow-ups (S208-03/S208-04), not attempted this pass. PARENA commit `f0048e8`. Apple #17105. (sess-20260830-1207-cc0ba7da)
- S208-06: PARENA stdlib/base4/matrix.prn -- STACK/MATMUL over base4 matrices (GRAMMAR.md §5.2), flat row-major shape matching array.prn's own NDArray convention. stack2 (2-row, matching every real matrix example in the source spec)/matrix-eq/dims-eq/matmul. Two real compiler gaps found and flagged, neither silently patched: sibling same-named loop bindings collide at C emission; matmul's accumulator hits the same double-boxing bug already confirmed on base4/vector.prn's dot. make test-base4-matrix green, -Werror clean. PARENA commit 8f56f97. Apple #17107. (sess-20260830-1207-cc0ba7da)
- README art: founder real-time ("OK -> https://okemily.com/prompt-o-verse/emily-emoji/ make that the readme art for LO use proper imagemagick"). Fetched the real gendata image from that prompt-o-verse taxonomy node (emily-emoji-optimized.jpg, 1024x1024), processed with ImageMagick (resize to 800x800, strip metadata, PNG for lossless README display) into assets/lo-readme-art.png, wired into README.md's own header. Source-image provenance kept here rather than a duplicated raw copy in the repo. (sess-20260830-1207-cc0ba7da)
- S208-07: PARENA stdlib/base4/pattern.prn -- real PCRE-lite pattern matcher over base4 vectors (GRAMMAR.md §5.4), backtracking architecture directly reused from PARENA's own regex/pcre.prn. Covers the source spec's one fully-worked pattern example (anchors, literal, wildcard, all four quantifiers); ALT/GROUP deferred (S208-09). Two new confirmed instances of the I32-boxing compiler gap already documented on S208-05/S208-06 found and worked around; a real, live OneOrMore off-by-one bug found via the file's own PARENA-native self-test, not glossed over. Real "write the main in pure PARENA" milestone (founder real-time): self-test holds all real test logic in .prn source since parena-c has no main-emission convention yet (confirmed against src/emit.c) -- the external C test is a one-call shim. make test-base4-pattern green, -Werror clean. PARENA commit 4fde48d. Apple #17121. (sess-20260830-1207-cc0ba7da)
- S208-09:PARENA stdlib/base4/pattern.prn 新增 ALT/GROUP 支援(GRAMMAR.md §5.4)。`elem-group`:真正、zero-width 的 GROUP(🗜)邊界標記,誠實聲明目前沒有 capture 數值擷取(還沒有下游需要)。`is-match-alt`:對兩個分開的 pattern Vec 做二元選擇,語意跟 PARENA 自己的 `regex/pcre.prn` 一致;比 GRAMMAR.md 描述的單一 flat Vec 內嵌 ALT token 窄,N 元選擇跟真正的內嵌 token 形式列為後續項目。新增真實的 GROUP/ALT self-test 案例。make test-base4-pattern 全綠、-Werror 乾淨。PARENA commit 74104d3。Apple #17131。(sess-20260830-1207-cc0ba7da)
- Phase 1(LO/NORTHSTAR.md)真正開工:第一個真實的元件是 lexer(`internal/lexer`,Go)。實作 GRAMMAR.md §1 的完整詞法規則——嚴格依照 §1.1 的 codepoint 對照表,§1.2 的 variation-selector 比對規則(去掉結尾單一個 VS15/VS16 再查表;ZWJ 緊接在 token 後面視為致命錯誤,不會被默默吞掉),VECLIT(`vec PARENA CONSTRUCT 312`)整段當一個 token 比對、不拆成單字。5 個真實測試全過:對照 GRAMMAR.md §7.1 自己的手算範例、對照這個 repo 自己既有的 `examples/xor_check.llll` 範例檔、驗證 bare 與 VS16 兩種寫法會被視為同一個 token、驗證 ZWJ 會真的報錯、驗證 VECLIT 完整比對與非法字元會報錯。`GOWORK=off go build/test/vet ./...` 全部乾淨。真正、誠實的範圍:目前只有 lexer,parser/emitter(Phase 1 真正的驗收標準——把 spec 自己的 on-thing 範例編譯成 `.prn`,再讓 `parena build`/`burrow build` 都吃得下去)還沒做,是下一步。(sess-20260830-1207-cc0ba7da)
- Phase 1(LO/NORTHSTAR.md)真正達成核心驗收標準:新增 parser(`internal/parser`)、emitter(`internal/emitter`)、真正的 CLI 進入點(`cmd/lo`,`lo build in.llll -o out.prn`)。Parser 實作 GRAMMAR.md §2 EBNF 裡目前有覆蓋到的子集(Door/Ternary/Cond/Arith/State/Void),過程中發現一個真實的落差:既有的 `examples/xor_check.llll` 用的是前綴記法(XOR4 S1 S3),但 GRAMMAR.md 自己的規格跟原始設計文件都是中綴(S1 XOR4 S3)——沒有自作主張改掉任何一邊,而是另外用一個跟 GRAMMAR.md 一致的例子測試,並把這個落差誠實記錄下來留待之後決定。Emitter 呼叫真正的 `PARENA/stdlib/base4/algebra.prn`(不是自己重新生出 base4 邏輯),真正端到端驗證過,不只是編譯過:`lo build` 出來的 `.prn` 真的餵給 `parena build` 產生 C,編譯、執行,結果正確(1 XOR 3 = 2,跟手算一致)。`burrow build` 這條路目前會失敗並誠實 skip(不是這個 emitter 的 bug——burrow 目前完全沒有跨檔案 import 解析,是它自己真實、獨立、還沒開始的缺口)。額外真實發現:把 defn 取名叫 "main" 會意外變成真正可執行的 C `int main`——不是 PARENA 刻意設計的進入點機制(直接翻過 src/emit.c 確認完全沒有對 "main" 這個字串做任何特殊處理),純粹是因為 mangler 剛好沒動到這個沒有連字號的名字,巧合撞上 C 自己的真正進入點名稱,老實記錄不是保證行為。`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
- 新增 FRAMEWORK_NORTHSTAR.md:LO「batteries included」Rails 風格框架的規劃文件,作為 SHITHUB(自建 GitHub,IDUNA 當 IAM)+ SHITLAB(仿 GitLab CI API)的 dogfooding 載體。只有規格,還沒寫任何框架程式碼——先老實檢查過 LO 目前真正能做到什麼:沒有變數/let、沒有多參數函式、沒有 record、沒有字串,框架至少要等 Phase 2(qi)落地,部分還要等 Phase 3+(String/Pattern)。真實的元件對應:router 重用 PARENA/stdlib/base4/pattern.prn 的 backtracking 架構(目前是 base4 向量不是字元,編碼方式怎麼接還沒定案,已列為開放問題);models 靠 qi 未來的 defstruct + IDUNA 儲存(不是另外做一套儲存引擎);migration 直接重用這次已經做好的 stdlib/papercraft/note_version_mod.prn 的合併/淘汰/衝突判斷邏輯。SHITHUB/SHITLAB 依照創辦人親自訂正過的分工(IAM 用 IDUNA 不是 PARENA;SHITLAB 是 CI API 形狀,不是第二個 git host)。「mono emoji」命名慣例(很可能是指單色/線框版的 emoji)列為真實、還沒解決、需要創辦人確認的問題,之後真的要加新 token 要走 GRAMMAR.md 修訂流程。真實分階段計畫:Phase A(卡在 LO Phase 2)→ Phase B(只做 router 的驗證)→ Phase C(只有設計:models/migration/SHITHUB git 邏輯)。已登記進 golden-docs-index(LO-FRAMEWORK-NORTH)。(sess-20260830-1207-cc0ba7da)
- FRAMEWORK_NORTHSTAR.md 的 Phase B 真正達成:PARENA/stdlib/http/router.prn(founder real-time: "ok well write the deps in parena")。真正、簡化版的路由比對 + `:name` 參數擷取,刻意不重用 base4/pattern.prn(那個是 base4 向量,不是字元)。過程中靠一次真正的 segfault 抓到並修好一個原本潛藏的 PARENA compiler/stdlib bug(vec-string-at 雙重解參考,regex/pcre.prn 自己那份一樣的 bug 也一起修了)。make test-http-router 全綠。PARENA commit ff92d61。Apple #17146。(sess-20260830-1207-cc0ba7da)
- 要求 LO 程式結尾一定要有分號(founder real-time: "also require semicolons in LO")。GRAMMAR.md 新增 SEMI(`;`)token,`Program ::= TypedExpr SEMI` 取代原本沒有終止符、單純靠輸入結束來判斷的規則。lexer/parser 都跟著改:少了結尾分號會是真正、明確的 parse error("expected a trailing SEMI"),不是靜默接受。§7 的三個既有範例(都是分號規則加入之前的舊例子)加了一段誠實的說明,標明現在都需要補上結尾的 `;` 才能通過目前的文法。真實驗證過:`lo build` 對有分號的程式正常編出 `.prn`,對沒有分號的程式會直接、清楚地報錯,不是編出錯的東西。新增/更新 4 個 parser 測試(含分號)、2 個 emitter 測試(含分號)。`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
- 真正的 `let`(變數綁定)進了 LO(founder real-time: "actually use ⚓ as the missing equality emoji EQ in the LO grammar and then use ✨ for LET")。EQ 的正式 glyph 從暫定的 🟰 改成確認過的 ⚓(錨,U+2693)——GRAMMAR.md §1.3 的懸而未決項目正式關閉;既有的 `examples/xor_check.llll` 也跟著機械式更新(語意沒變,只是換符號)。新增 ✨(U+2728)當作真正的 LET token,GRAMMAR.md §2 新增 `Let ::= LET Value Expr` 文法。真實的架構簡化:LO 自己的 `let` 不用原始 spec 那套 De Bruijn/environment-matrix 的複雜 lowering 方式(spec 自己承認的「炸成一長串」爆炸風險)——因為 LO 現在真正的目標是 PARENA,PARENA 本來就有真正能用的 `let`,所以直接 lower 成 `(let [x v] body)` 就好,完全繞開那個爆炸風險。真實、刻意縮小的範圍:一次只有一個有效綁定,巢狀 `let` 會遮蔽外層(用同一個變數名 `x`,PARENA 自己的 `let` 本來就會正確遮蔽,LO 這邊完全不用額外處理)。裸的 MAGENT(🧲,沒有運算元)代表「最內層 let 綁定的值」,跟原本 `VectorLit MAGNET Value`(列取出)的用法分開。過程中抓到並修好一個真實的 parser bug:裸 MAGNET 一開始沒有檢查後面接不接算術運算子,會把 `🧲 🔀 🌒` 的 `🔀 🌒` 整段吃掉不見——比對 `State` 那邊本來就有的邏輯才發現。真正端到端驗證過,不只是形狀檢查:`✨ 🌓 🧲 🔀 🌒` 編出來的 `.prn` 真的丟給 `parena build` + `cc` + 執行,結果正確(S2 XOR4 S1 = 3)。15 個測試全過(4 個新增:TestParseLet、TestParseNestedLetShadows、TestEmitLetRunsCorrectly,加上既有測試因為 glyph 換掉而更新)。`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
- 修好一個真實的、還沒被抓到的 parser 缺口:算術運算子之前只吃得下「一個」運算子,而且右邊運算元只能是裸的 State——這代表 `S1 XOR4 S2 XOR4 S3`(GRAMMAR.md §3.3 自己講的「左結合」鏈)根本解析不了,`x XOR4 y`(兩個 let 綁定的值)也解析不了。改成真正的左結合鏈式解析(`value()` 迴圈吃 `(op, primary)`,`primary()` 統一處理 State/VOID/裸 MAGNET)。誠實補充:兩個 MAGNET 出現在同一段身體裡目前還是指向同一個(最內層的)綁定——因為巢狀 let 是完全遮蔽外層,不是「同時看得到兩個變數」,這個限制已經在 GRAMMAR.md 自己的 Let 說明裡點名,不是這次才發現的新限制。真正端到端驗證過:`S1 XOR4 S2 XOR4 S3` 真的编過 `parena build` + `cc` + 執行,結果正確((1^2)^3 = 0)。新增/調整 4 個 parser 測試、1 個真正跑過編譯執行的 emitter 測試(把原本 TestEmitLetRunsCorrectly 的重複 subprocess 邏輯抽成共用的 compileRunAndGetExitCode helper)。18 個測試全過,`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
- 完成 `let` 的最後一塊拼圖:讓巢狀 `let` 也能真的碰到外層的綁定,不再只能碰到最內層。裸的 `🧲` 還是指最內層(Depth 0,完全向下相容),新增 `vec PARENA CONSTRUCT 312 <單一狀態> 🧲` 這個形式,狀態值就是要往外數幾層——這是直接重用 GRAMMAR.md 自己本來就定義好的 MagnetExpr token 形狀(index-vector 緊接在 MAGNET 前面),只是簡化成單一狀態當作深度索引,不是完整的列取出。Emitter 這邊改成用 `x0`、`x1`... 這種按巢狀深度命名的變數,不再是每一層都用同一個 `x`(反正 PARENA 自己的 `let` 本來就會正確處理 shadowing,現在不需要靠它了,直接給每層自己的名字)。真正端到端驗證過,不只是形狀檢查:外層綁 S2、內層綁 S1,內層身體用 depth-1 的 LetRef 拿到外層的 S2、bare MAGNET 拿到內層的 S1,兩個 XOR 起來真的編出去、跑出來,結果正確(2^1=3)。新增 2 個 parser 測試、1 個真正跑過編譯執行的 emitter 測試,20 個測試全過,`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
- 加入真正的 SWITCH/CASE/DEFAULT 跟 LAMBDA/CALL(founder real-time: "add switch and case" / "🐪 LAMBDA" / "add ... LAMBDA")。創辦人上傳了一份完整、後出、更權威的 Phase 0 文法整合文件(`LO_Formal_Grammar_Phase_0_Complete.md`,1316 行,加了 SWITCH/CASE/DEFAULT、LAMBDA/CALL、FLOAT/DOUBLE,還有更完整的 PCRE literal/class/range/escape 文法)——這份文件現在是 Phase 0 的正式規格,GRAMMAR.md 改成追蹤「實際做出來的東西」跟過程中真正發現的 compiler bug。誠實揭露一個真實衝突,沒有自己偷偷解決掉:上傳文件的 §33 明講「巢狀 let 存取被遮蔽的外層綁定」屬於 v1 範圍外,但這個 repo 自己這週的 S221-01 早就做出來而且已經上線了——選擇保留這個已經測試過、能動的超集,不是回頭砍掉,只是誠實記下來讓創辦人知道。真正實作 SWITCH/CASE/DEFAULT(🔘/🔹/🔸,lower 成巢狀的 if/=)跟 LAMBDA/CALL(💠/📞,LAMBDA 沿用 Let/LetRef 既有的深度索引綁定機制,因為真正的帶引號字串字面值 lexing 還沒做;LAMBDA/CALL 目前都只支援單一參數/單一引數,文件裡的多參數/多引數留給之後)。真正端到端驗證過,不只是形狀檢查:SWITCH 完全照文件自己 §18 的範例編出來、跑出來,結果正確(3);LAMBDA+CALL 也真的編出一個立即呼叫的 `((fn [(x:I32)] body) arg)`,PARENA 真的接受這種寫法,執行結果正確(2^1=3)。新增 6 個 parser 測試、2 個真正跑過編譯執行的 emitter 測試。26 個測試全過,`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
- 加入真正的 FLOAT/DOUBLE Door 型別支援(⚫/⚪,LO_Formal_Grammar_Phase_0_Complete.md §7.2/7.3,founder real-time: "continue")。真實、查證過而非猜測的發現:PARENA 自己完全沒有獨立的單精度浮點型別(直接翻過 src/emit.c,找不到任何 "F32"),所以 LO 的 FLOAT 跟 DOUBLE 兩個型別最終都對應到同一個真正的 PARENA F64——這個 v0 底層每個 LO 運算式仍然是 base4-state 的 I32 算術,所以 FLOAT/DOUBLE Door 是把最終的 I32 結果透過一個新增、產生在同一個模組裡的小型 `lo-i32-to-f64` helper(`#target {:c (inline-c "(double)(x)")}`)轉型成 F64,不是真的新增一種浮點運算式型別。過程中發現一個真實、之前沒遇過的驗證陷阱:`(defn main [] : F64 ...)` 會被 mangle 成字面上的 C `double main(void)`,可以編譯(會有 `-Wmain` 警告)但它的 process exit code 是未定義行為(呼叫慣例不合——作業系統的 exit status 是整數欄位,不是浮點數回傳值),所以既有的、靠 exit code 驗證的測試手法在這裡完全不能用。解法:先用純 shell/`cc` 手動驗證,再寫進 Go 測試——用真正標準的 C 前處理器手法,`#define main lo_generated_main` 把產生的 .c 檔裡衝突的 `main` 符號改名後再 `#include`,自己另外寫一個真正的 `int main(void)` 直接呼叫改名後的函式、用 `printf` 印出真正的浮點數值,測試再從 stdout 解析這個值,不依賴 exit code。新增 `compileRunAndGetFloat` 這個共用 helper(從既有的 `compileRunAndGetExitCode` 抽出共用的 `compileToGeneratedC` 前半段,避免重複 lex→parse→emit→`parena build` 邏輯),2 個真正跑過編譯執行的新測試(`TestEmitFloatDoorRunsCorrectly`:`🚪 ⚫ 🌒;` 得到 1.0;`TestEmitDoubleDoorRunsCorrectly`:`🚪 ⚪ 🌒 🔀 🌓;` 得到 3.0,1 XOR4 2 = 3 跟手算一致)。GRAMMAR.md 同時補上先前一直缺漏的 EBNF 條目(TypeAtom 的 FLOAT/DOUBLE,以及 SWITCH/CASE/DEFAULT、LAMBDA/CALL 本身——這兩組先前只在標頭說明裡提過已實作,正式文法區塊還沒補上)。28 個測試全過,`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
- 開始啃 LO_Formal_Grammar_Phase_0_Complete.md §19-29 那份更大的 PCRE literal/class/range/escape 文法(founder real-time: "continue"),先從 lexer 這一層下手:新增 LITERAL(🔤)、CLASS(🅰️)、NCLASS(🚫)、RANGE(↔️)、ESCAPE(🛡️)五個 token。LITERAL 比較特殊,不是單純的 emoji 對照——後面要真的接一段用雙引號包起來的文字(`🔤"cat"`),所以新增 `lexQuotedText`,把文字內容存進 `Token.Text`(新欄位)。真實、誠實記下來的決定,因為原始文件自己完全沒講清楚這段引號規則:文件裡每一個範例都是 🔤 跟開頭雙引號中間完全沒有空格,所以這裡規定 🔤 後面一定要立刻接雙引號、中間不能有空白(這是刻意違反 §1 自己「token 之間空白不影響」的通則,原始文件沒給別的線索所以只能這樣定);引號裡面不處理任何反斜線跳脫(文件自己的 ESCAPE token 是 PATTERN 層級的、套用在後面的 Escapable 上,不是字串本身的跳脫);沒有結尾雙引號是真正、明確的 lex error。誠實範圍:這次只做到 lexer 認得出這些 token 跟正確讀出引號內容,parser/emitter 那邊真正的 Pattern/Literal/CharacterClass/Escaped 文法,還有 MATCH 需要的真正字串型別 Value(LO 目前完全沒有字串值),都是下一步、還沒做。新增 4 個 lexer 測試(含空白/未關閉引號的錯誤案例)。32 個測試全過,`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
- 接著把 PCRE 文法(§19-29)往前推到 parser 這一層(founder real-time: "continue")。新增 `pattern`/`patternSequence`/`patternItem`/`patternAtom`/`patternClass`/`patternEscaped` 這一組真正的 parser 函式,對照文件自己 §20-29 每一個範例逐一驗證過(Literal、Wildcard、三種 Quantifier、START/END 錨點、Alternation、CLASS/NCLASS+Range、Escaped、可加 Quantifier 的 PatternGroup)。掛進 `Cond ::= Value MATCH Pattern`(新增 `Match` 這個 AST node,跟既有的 `Eq` 平行)。兩個真實、誠實記下來而不是自己偷偷決定的取捨:文件自己的 `Atom` 定義裡有一個裸的 base4 `State`,但 §20-29 每一個範例都只用文字類的 atom——這裡判斷是 LO 舊版 base4-vector pattern 文法留下來的殘留(完全不同的概念),所以沒有實作這個分支;另外巢狀 `PatternGroup` 目前結構上還是解析得過,雖然 §33 講巢狀 capture group 不在 v1 範圍內——還沒加深度檢查,列為真實的後續項目。過程中抓到並修好一個真實的 parser bug:GROUP(🗜️)同一個 token 兼做開跟關,原本判斷「這是不是一個新 pattern item 的開頭」時沒有分身在群組裡面還是外面,導致群組自己的結尾符號被誤判成又開了一個新的巢狀群組——改成 `insideGroup` 這個參數,群組本體裡面遇到 GROUP 一律當作是自己的結尾,不會再誤開新群組。誠實範圍:這次還是只到 parser,emitter 完全還沒接、LO 也還沒有真正的字串值,所以只做到 AST 形狀驗證,還沒有東西可以真正端到端跑。新增 10 個 parser 測試。41 個測試全過,`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
- 把 Pattern AST 真的轉成 PCRE 語法文字(founder real-time: "continue"),新增 `internal/emitter/pattern.go` 的 `patternToPCRE`。這就是文件自己 §20-29 每個範例旁邊寫的「等效 PCRE」那段文字,也剛好是 PARENA 自己那份真正、成熟的 `regex/pcre.prn`(`compile`/`is-match`,S222-05 已經確認存在)實際吃得下去的格式。逐一比對文件自己每一個範例,產生出來的文字要跟文件寫的一模一樣(`^cat$`、`c.t`、`a*`/`a+`/`a?`、`cat|dog|fox`、`[abc]`/`[^abc]`、`[a-zA-Z0-9]+`、`(ab)+`、跳脫過的 `\*`/`\.`),另外自己額外驗證一個文件沒範例覆蓋到的正確性:LITERAL 裡面如果本來就帶有 PCRE 的特殊字元,輸出時要自動加反斜線跳脫(§20 自己講「引號內容是字面上的文字」,不是 PCRE 語法,所以不能讓字面文字被誤判成語法)。誠實記下一個真實、沒有硬猜的缺口:`ESCAPE GROUP`(跳脫括號)在這個 v0 沒辦法唯一決定要輸出 `(` 還是 `)`——parser 自己記錄的 `PatternEscaped` 只知道「括號這個 token 被跳脫了」,完全不知道原意是開括號還是關括號(因為 GROUP 開跟關本來就共用同一個 token),所以這裡選擇誠實報錯,不是隨便猜一個。誠實、還沒做的下一步,老實列出來而不是含糊帶過:要把這段 PCRE 文字真的接進 `.prn` 呼叫 `regex/pcre/compile`+`is-match`,需要呼叫端自己傳一個 `Arena @ Region` 進去——這會直接打壞 LO 現有「把 defn 取名叫 main 借用 C entry point」這招的前提(那招需要零參數的函式;對照 `PARENA/tests/test_base4_pattern.c` 確認過,PARENA 自己真正需要 Arena 的 self-test 慣例,都是靠外部的 C driver 把 Arena 傳進去,從來不會在零參數的 main 裡面自己生一個出來),另外 LO 目前也完全沒有真正的字串型別值可以當 MATCH 的主體——這兩個都還是真實、獨立、之後才做的後續項目。新增 24 個 emitter 測試(目前只到字串輸出這一層,還沒有真正的 `.prn` 產出可以編譯執行)。65 個測試全過,`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
- LO 有了第一個真正的字串型別值(founder real-time: "continue")。新增 `StringLit`:裸的 `LITERAL`(🔤"...")現在可以直接當一般的 `Value` 用,不再只能出現在 Pattern 裡面。誠實記下來的決定:原始文件自己的 `Value` 定義列表裡其實沒有列出裸的 `Literal`(只有 `PatternValue`)——文件自己每一個 STRING Door 的範例(§5)寫的都是英文佔位字 "value",從來沒有給過真正的 LO 原始碼寫法,所以這是一個真實、有先聲明過的最小延伸,不是自己亂猜文件沒講的東西。真正端到端驗證過,不只是形狀檢查:`🚪 📜 🔤"hello";` 真的編過 `parena build` + `cc` + 執行,印出 `hello`——沿用 FLOAT/DOUBLE 那次已經建立好的 `#define main` 改名驅動手法(PARENA 的 `String` 直接對應 C 的 `char *`,對照 `src/emit.c` 確認過;`char *main(void)` 跟當初 F64 一樣,process exit code 沒有意義)。過程中抓到並修好一個真實、查證過的正確性問題:PARENA 自己的字串讀取器會把 `\n`/`\t`/`\"`/`\\` 這些跳脫序列解開(對照 `src/lexer.c` 的 `lex_string` 確認過),但 LO 自己的 `lexQuotedText` 完全不處理引號裡面的跳脫——所以字面上的反斜線在丟給 PARENA 之前一定要先變成兩個反斜線,不然會被 PARENA 誤判成一個沒打算要的跳脫序列;真的用 `a\b` 這個例子端到端跑過確認正確。誠實補充:MATCH/Pattern 真正接上 `regex/pcre` 呼叫還是需要先解決前一次提到的 Arena 傳遞問題,這次的字串值工作等於把那個問題變成唯一剩下的真正阻礙,不是又多一個。新增 1 個 parser 測試、2 個真正跑過編譯執行的 emitter 測試。65 個測試全過(`go test -v` 逐一數過,含 subtest),`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
- MATCH 終於真正端到端跑起來了(founder real-time: "continue")。`exprNeedsArena` 會掃過整棵運算式樹找 `Match`,一旦找到,產生出來的函式就不再叫 `main`,改叫 `lo-program`(因為需要一個真正的 `(dest : Arena @ Region)` 參數,`int main(Arena)` 根本不是合法的 C 進入點簽名,跟當初 F64/String 那種「簽名怪但至少合法」的情況不一樣),另外加上 `(import regex/pcre)`。`Match` 現在會 lower 成跟 `stdlib/awk.prn` 自己的 `rule-matches?` 一模一樣的 `match`/`Ok`/`Err`/`unbox-bool` Result 解包寫法,真的呼叫 PARENA 自己的 `regex/pcre/compile`+`is-match`,吃 `patternToPCRE` 產生的文字跟一個 `StringLit` 主體。過程中抓到兩個真實、活生生的 PARENA compiler 問題,這次選擇繞過而不是動 `src/emit.c` 本體(那是真正、獨立、超出這次 LO 範圍的調查):第一,`{:max-steps N}` 這種 `MatchBudget` map 字面值不能直接當呼叫參數丟進去——`parena build` 真的會報錯說「map 字面值對不上任何已註冊 defstruct 的欄位組合」——所以跟每一個真正的 stdlib 呼叫端一樣,先用 `let` 綁起來;第二,這個是真正的 compiler bug,不只是寫法上的不方便:任何綁定形式(`let`,或是 `match` 某個分支自己綁的名字)只要直接拿來當 `if` 的條件式,裡面只要有引用到那個綁定的名字就會報「unknown identifier」——即使同一段綁定寫法拿來當函式本體或當 `let` 自己的 body 都編譯得過。針對 `let` 跟 `match` 兩種情況都直接對著 `parena build` 做過最小重現,確認是同一類問題。解法是先把整條 match 鏈完整算出來,存進一個普通的 `result` 綁定(這是正常的 `let` body 位置,編譯得過),`if` 的條件式就只是引用一個已經算好的名字,不再是綁定形式本身。真正端到端驗證過三個案例:`"cat" MATCH ^cat$` → true、`"dog" MATCH ^cat$` → false、`"42" MATCH [0-9]+` → true(這個順便也真的跑過 `patternToPCRE` 自己的 class/range/quantifier lowering,不只是字串輸出的單元測試)。測試用的 `compileToGeneratedC` 也跟著改:每次都把 `regex/pcre` 真正需要的整組依賴(`string`/`array`/`io`/`regex/syntax`/`regex/pcre`)一起丟給 `parena build`——因為 PARENA 是整個 build 指令列出的檔案一起編譯,生成的 `.prn` 裡面單純寫 `(import ...)` 是不夠的,對不用 MATCH 的程式也沒有額外成本。新增 3 個真正跑過編譯執行的 emitter 測試。68 個測試全過,`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
- MATCH 的主體現在可以是 LET 綁定過的字串了(founder real-time: "continue")。`emitExpr` 新增一個很小的、按綁定位置追蹤型別的機制(`exprType`,一個跟 `x0`/`x1`... 用同一套索引方式對應的 `types` 陣列——沒被 LET 綁定過的位置一律維持隱含的 `typeI32`,跟之前完全一樣的假設)。`Let` 現在會記下自己 Bound 那段推論出來的型別(`exprTypeOf`:`StringLit` 是 `typeString`,`LetRef` 透過 `types` 往回查,其餘一律 `typeI32`),讓 Body 那一層知道。`MATCH` 自己的主體(`emitStringSubject`)現在除了裸的 `StringLit`(不變)以外,也接受一個真的指向 `typeString` 綁定的 `LetRef`/`MAGNET`——把上一次(S222-09)留下來的「主體只能是裸 LITERAL」限制關掉了。如果 `LetRef` 指到的是 `typeI32` 的綁定,還是誠實報錯,不會偷偷轉型。真正端到端驗證過:`LET "cat" (MAGNET MATCH ^cat$) ? S1 : S0` 真的編過、跑過,結果正確(有 match)。誠實補充還沒做的部分:`Lambda` 自己的參數永遠是 `typeI32`(它真正的型別要等 CALL 的時候才知道,而且 PARENA 的 `fn` 本來就需要在編譯時就決定好參數的具體型別)——字串型別的 Lambda 參數是另外一塊,更大、還沒開始的後續工作。新增 2 個真正的 emitter 測試(一個真的跑過的 match、一個誠實報錯的 I32-LetRef-當主體案例)。70 個測試全過,`GOWORK=off go build/vet/test ./...` 全部乾淨。(sess-20260830-1207-cc0ba7da)
